-*- mode: org -*-

* --follow or --keep-pinging or --continuous
So, given that --gateway is super handy, it might be nice to allow for simple
pinging (i.e., single process w/out -c1).  Probably need it to be mutually
exclusive from the spawn a million single-ping processess way of doing things.
